package com.kooo.evcam.dingtalk;

/**
 * DISABLED: This class uses internal DingTalk SDK protocol classes that are not available
 * in the public API (Frame, Message, EventAckRequest, etc.)
 *
 * If you need this functionality, you'll need to:
 * 1. Use a different version of the DingTalk SDK that exposes these classes
 * 2. Or rewrite this to use only the public API
 *
 * For now, use DingTalkStreamManager instead which uses the public API.
 */
public class DingTalkStreamClientV2 {
    // Class disabled due to missing SDK dependencies
}
